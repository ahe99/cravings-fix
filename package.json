{
  "name": "e-commerce",
  "private": true,
  "scripts": {
    "dev": "pnpm -r run dev",
    "start": "pnpm -r start",
    "build": "pnpm --filter=!api build",
    "dev:client": "pnpm run -C app/client dev",
    "dev:api": "pnpm run -C app/api dev",
    "dev:admin": "pnpm run -C app/api dev"
  },
  "dependencies": {},
  "devDependencies": {
    "prettier": "^2.8.8",
    "pretty-quick": "^3.1.3"
  },
  "husky": {
    "hooks": {
      "pre-commit": "pretty-quick --no-verify --staged"
    }
  }
}
